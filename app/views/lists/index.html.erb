<h1>Lists#index</h1>
<p>Find me in app/views/lists/index.html.erb</p>

<%= link_to "Create a list", new_list_path%>

<div class="container">
  <% @lists.each do |l| %>
    <%= l.name %>
    <%= link_to "see movies in list", list_path(l)%>
  <% end %>
</div>
